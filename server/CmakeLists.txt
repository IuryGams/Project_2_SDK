cmake_minimum_required(VERSION 3.21)

add_executable(
    server

    server.cpp
)


target_include_directories(
    server

    PUBLIC
        ${CMAKE_CURRENT_SOURCE_DIR}/../include
        ${CMAKE_CURRENT_BINARY_DIR}/../proto
)

target_link_libraries(
    server

    PUBLIC
        PROJECT_INTEGRATION_lib
        gRPC::grpc++
        protobuf::libprotobuf
)
